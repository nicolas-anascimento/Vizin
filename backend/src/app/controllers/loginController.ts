import { login, register, logout } from "./authController.ts";
// Alias legado: mantém a antiga importação de login apontando para o controller de autenticação.
export default { login, register, logout };
